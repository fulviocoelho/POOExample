import IDatabaseInfo from "../interfaces/IDatabaseInfo";

const database_connect_info: IDatabaseInfo = { 
    user: 'user', 
    pass: '123456', 
    host: 'local' 
}

export default database_connect_info;